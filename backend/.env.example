# MongoDB Connection String
# For MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/dbname
# For Local MongoDB: mongodb://localhost:27017/tododb
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/tododb?retryWrites=true&w=majority

# Server Port
PORT=3001

# JWT Secret Key (use a random string, min 32 characters)
JWT_SECRET=change-this-to-a-random-secret-key-at-least-32-chars

# Node Environment
NODE_ENV=development

# Frontend CORS Origin
CORS_ORIGIN=http://localhost:5173

# Optional: Database name (if not in MONGO_URI)
# DB_NAME=tododb
